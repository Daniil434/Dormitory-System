<Window x:Class="Dormitory_System.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:b="clr-namespace:Dormitory_System.Behaviors"
        xmlns:local="clr-namespace:Dormitory_System.Views" 
        xmlns:vm="clr-namespace:Dormitory_System.ViewModels"
        mc:Ignorable="d"
        Title="Авторизация" Height="450" Width="800">
    
    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Вход в систему общежития" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" HorizontalAlignment="Center"/>

        <!-- Логин -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10">
            <Label Content="Пользователь:" Width="100"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
        </StackPanel>

        <!-- Пароль -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10">
            <Label Content="Пароль:" Width="100"/>
            <PasswordBox Name="PasswordBox" Width="200" Height="25"
                         PasswordChanged="PasswordBox_PasswordChanged"/>
        </StackPanel>

        <Button Grid.Row="4" Content="Войти" Command="{Binding LoginCommand}" Width="150" HorizontalAlignment="Center"/>
    </Grid>
</Window>
